import { FiltroReseniaAsignaturaPipe } from './filtro-resenia-asignatura.pipe';

describe('FiltroReseniaAsignaturaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroReseniaAsignaturaPipe();
    expect(pipe).toBeTruthy();
  });
});
