import { FiltroExisteReseniaPipe } from './filtro-existe-resenia.pipe';

describe('FiltroExisteReseniaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroExisteReseniaPipe();
    expect(pipe).toBeTruthy();
  });
});
