import { FiltroActividadesPipe } from './filtro-actividades.pipe';

describe('FiltroActividadesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroActividadesPipe();
    expect(pipe).toBeTruthy();
  });
});
