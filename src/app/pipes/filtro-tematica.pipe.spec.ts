import { FiltroTematicaPipe } from './filtro-tematica.pipe';

describe('FiltroTematicaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTematicaPipe();
    expect(pipe).toBeTruthy();
  });
});
